{
  "version": 2,
  "builds": [
    { "src": "./*.csproj", "use": "@vercel/dotnet" },
    { "src": "ClientApp/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "http://localhost:7149/api/$1" },
    { "src": "/(.*)", "dest": "/ClientApp/$1" }
  ]
}
